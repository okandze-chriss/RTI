var primary = localStorage.getItem("primary") || '#b7ce02';
var secondary = localStorage.getItem("secondary") || '#f18708';

window.CubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
